using UnityEngine;

public class MetalStairs : MonoBehaviour
{

    public float var = 1.0f;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void Action()
    {
        
    }
}
